# picamera2 is installed via apt (python3-picamera2) NOT pip
# Use venv with --system-site-packages so the apt package is accessible
# Python >= 3.11 required (Pi OS Bookworm default)

HAP-python[QRCode]>=4.9.0,<6.0
PyYAML>=6.0
numpy>=1.24
opencv-python-headless>=4.8
tflite-runtime>=2.14.0
qrcode[pil]>=7.4
