# DEVELOPMENT ONLY. Production installs never touch pip: install.sh gets
# every Python dependency from apt (python3-yaml, python3-numpy,
# python3-opencv, python3-picamera2) and the service runs on the system
# python — no venv. This file exists for dev machines running pytest
# (numpy/cv2/picamera2 are mocked by tests/conftest.py, PyYAML is real).
PyYAML>=6.0
