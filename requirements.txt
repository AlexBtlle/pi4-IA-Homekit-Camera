PyYAML>=6.0
# numpy, opencv (cv2) and picamera2 are installed via apt
# (python3-numpy, python3-opencv, python3-picamera2) and accessed through
# the venv's --system-site-packages. They are NOT installed via pip because
# prebuilt wheels are unreliable across Raspberry Pi OS / Python versions.
